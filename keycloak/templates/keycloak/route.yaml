kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: keycloak
spec:
  path: /
  to:
    kind: Service
    name: composer-ai-rhbk-service
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
